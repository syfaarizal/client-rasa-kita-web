# Styles

Global styles dan design tokens (warna, radius, shadow) didefinisikan di `app/globals.css`
menggunakan Tailwind CSS v4 `@theme`. Folder ini disediakan untuk menyimpan stylesheet
tambahan (misalnya CSS module khusus) apabila proyek berkembang.
